<Window x:Class="CallBridge.Desktop.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="CallBridge" Height="780" Width="1240" MinHeight="640" MinWidth="820"
        WindowStartupLocation="CenterScreen" Background="#F3F8FC" Foreground="#102033"
        FontFamily="Segoe UI">
    <Window.Resources>
        <SolidColorBrush x:Key="Ink" Color="#102033"/>
        <SolidColorBrush x:Key="Muted" Color="#65788C"/>
        <SolidColorBrush x:Key="Line" Color="#D5E4EC"/>
        <SolidColorBrush x:Key="Panel" Color="#FFFFFF"/>
        <SolidColorBrush x:Key="PanelSoft" Color="#F7FBFE"/>
        <SolidColorBrush x:Key="Brand" Color="#087EB3"/>
        <SolidColorBrush x:Key="BrandDark" Color="#0B5E86"/>
        <SolidColorBrush x:Key="Accent" Color="#2FB7C7"/>
        <SolidColorBrush x:Key="Success" Color="#31B78D"/>
        <SolidColorBrush x:Key="Warning" Color="#DFAE4B"/>
        <SolidColorBrush x:Key="Danger" Color="#C95669"/>
        <DropShadowEffect x:Key="SoftShadow" Color="#6C8EA0" BlurRadius="24" ShadowDepth="6" Opacity=".18"/>

        <Style TargetType="Button">
            <Setter Property="Foreground" Value="{StaticResource Ink}"/>
            <Setter Property="Background" Value="#FFFFFF"/>
            <Setter Property="BorderBrush" Value="{StaticResource Line}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="13,9"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Root"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="8"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                              VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Root" Property="Background" Value="#EAF7FC"/>
                                <Setter TargetName="Root" Property="BorderBrush" Value="#83CADA"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="Root" Property="Background" Value="#DDF1F8"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Opacity" Value=".45"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="NavButton" TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
            <Setter Property="HorizontalContentAlignment" Value="Left"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="BorderBrush" Value="Transparent"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Padding" Value="13,11"/>
            <Setter Property="Margin" Value="0,2"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
        </Style>

        <Style x:Key="DialButton" TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
            <Setter Property="MinWidth" Value="70"/>
            <Setter Property="MinHeight" Value="54"/>
            <Setter Property="Margin" Value="5"/>
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="Background" Value="#F7FCFE"/>
            <Setter Property="BorderBrush" Value="#D2E6EF"/>
        </Style>

        <Style x:Key="PanelBorder" TargetType="Border">
            <Setter Property="Background" Value="{StaticResource Panel}"/>
            <Setter Property="BorderBrush" Value="{StaticResource Line}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="CornerRadius" Value="8"/>
            <Setter Property="Effect" Value="{StaticResource SoftShadow}"/>
        </Style>

        <Style x:Key="MetricCard" TargetType="Border" BasedOn="{StaticResource PanelBorder}">
            <Setter Property="Padding" Value="18"/>
            <Setter Property="Effect" Value="{x:Null}"/>
        </Style>

        <Style TargetType="ComboBox">
            <Setter Property="Padding" Value="8,4"/>
            <Setter Property="BorderBrush" Value="#C8DEE8"/>
            <Setter Property="Background" Value="#FFFFFF"/>
            <Setter Property="Foreground" Value="{StaticResource Ink}"/>
        </Style>

        <Style TargetType="TextBox">
            <Setter Property="Padding" Value="10"/>
            <Setter Property="BorderBrush" Value="#C8DEE8"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Background" Value="#FBFEFF"/>
            <Setter Property="Foreground" Value="{StaticResource Ink}"/>
        </Style>

        <Style x:Key="SoftListItem" TargetType="ListBoxItem">
            <Setter Property="Padding" Value="0"/>
            <Setter Property="Margin" Value="0,0,0,10"/>
            <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ListBoxItem">
                        <Border x:Name="ItemRoot" CornerRadius="8">
                            <ContentPresenter/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="ItemRoot" Property="Background" Value="#E9F7FC"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <DataTemplate x:Key="RowItemTemplate">
            <Border BorderBrush="#D7E7EF" BorderThickness="1" CornerRadius="8" Padding="14" MinHeight="64">
                <Border.Background>
                    <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                        <GradientStop Color="#FFFFFF" Offset="0"/>
                        <GradientStop Color="#F5FBFE" Offset="1"/>
                    </LinearGradientBrush>
                </Border.Background>
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="42"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto" MinWidth="78"/>
                    </Grid.ColumnDefinitions>
                    <Border Width="34" Height="34" CornerRadius="8" BorderBrush="#AFDAE7" BorderThickness="1">
                        <Border.Background>
                            <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                                <GradientStop Color="#E9F8FF" Offset="0"/>
                                <GradientStop Color="#EEF9F8" Offset="1"/>
                            </LinearGradientBrush>
                        </Border.Background>
                        <TextBlock Text="&#xE717;" FontFamily="Segoe MDL2 Assets" Foreground="{StaticResource Brand}" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Border>
                    <StackPanel Grid.Column="1" VerticalAlignment="Center">
                        <TextBlock Text="{Binding Title}" FontWeight="SemiBold" FontSize="13" Foreground="{StaticResource Ink}" TextTrimming="CharacterEllipsis"/>
                        <TextBlock Text="{Binding Detail}" FontSize="11" Foreground="{StaticResource Muted}" Margin="0,4,8,0" TextTrimming="CharacterEllipsis"/>
                    </StackPanel>
                    <Border Grid.Column="2" CornerRadius="8" Background="#E8F6FB" BorderBrush="#CBE5EF" BorderThickness="1" Padding="10,5" HorizontalAlignment="Right" VerticalAlignment="Center">
                        <TextBlock Text="{Binding Action}" Foreground="{StaticResource BrandDark}" FontWeight="SemiBold" FontSize="11" TextTrimming="CharacterEllipsis"/>
                    </Border>
                </Grid>
            </Border>
        </DataTemplate>
    </Window.Resources>

    <Grid>
        <Grid.Background>
            <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                <GradientStop Color="#F7FBFE" Offset="0"/>
                <GradientStop Color="#EEF7FB" Offset=".55"/>
                <GradientStop Color="#F5F8FB" Offset="1"/>
            </LinearGradientBrush>
        </Grid.Background>
        <Grid.ColumnDefinitions>
            <ColumnDefinition x:Name="SidebarColumn" Width="248" MinWidth="74"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Border BorderBrush="#CADFE9" BorderThickness="0,0,1,0">
            <Border.Background>
                <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                    <GradientStop Color="#FFFFFF" Offset="0"/>
                    <GradientStop Color="#F2FAFD" Offset=".62"/>
                    <GradientStop Color="#EAF4F8" Offset="1"/>
                </LinearGradientBrush>
            </Border.Background>
            <Grid x:Name="SidebarRoot" Margin="18">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <StackPanel x:Name="BrandBlock" Margin="0,6,0,18">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="50"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <Border Width="44" Height="44" CornerRadius="8" BorderBrush="#A9D7E5" BorderThickness="1">
                            <Border.Background>
                                <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                                    <GradientStop Color="#0E86BC" Offset="0"/>
                                    <GradientStop Color="#26B3C1" Offset="1"/>
                                </LinearGradientBrush>
                            </Border.Background>
                            <Grid>
                                <Image Source="Assets/ithealthtech-logo.png" Height="26" Stretch="Uniform" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                <TextBlock x:Name="LogoText" Text="CB" Foreground="White" FontWeight="Bold" HorizontalAlignment="Center" VerticalAlignment="Center" Visibility="Collapsed"/>
                            </Grid>
                        </Border>
                        <StackPanel Grid.Column="1" VerticalAlignment="Center">
                            <TextBlock x:Name="BrandTitle" Text="CALLBRIDGE" FontWeight="Bold" FontSize="15"/>
                            <TextBlock x:Name="BrandSubtitle" Text="IT HEALTH TECHNOLOGIES" Foreground="{StaticResource Muted}" FontSize="9" Margin="0,4,0,0"/>
                        </StackPanel>
                    </Grid>
                </StackPanel>

                <Border Grid.Row="1" BorderBrush="#BFDDE8" BorderThickness="1" CornerRadius="8" Padding="13" Margin="0,0,0,14">
                    <Border.Background>
                        <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                            <GradientStop Color="#F0FBFF" Offset="0"/>
                            <GradientStop Color="#F7FAFC" Offset="1"/>
                        </LinearGradientBrush>
                    </Border.Background>
                    <StackPanel Orientation="Horizontal">
                        <Ellipse x:Name="PresenceDot" Width="10" Height="10" Fill="{StaticResource Accent}" Margin="0,4,10,0"/>
                        <StackPanel x:Name="PresenceDetails">
                            <TextBlock x:Name="PresenceText" Text="Available" FontWeight="SemiBold" FontSize="13"/>
                            <TextBlock x:Name="ExtensionText" Text="Extension 201" Foreground="{StaticResource Muted}" FontSize="11" Margin="0,3,0,0"/>
                        </StackPanel>
                    </StackPanel>
                </Border>

                <Grid Grid.Row="2">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
                        <StackPanel x:Name="NavStack">
                            <Button x:Name="DashboardButton" Style="{StaticResource NavButton}" Content="Dashboard"/>
                            <Button x:Name="PhoneButton" Style="{StaticResource NavButton}" Content="Phone"/>
                            <Button x:Name="ContactsButton" Style="{StaticResource NavButton}" Content="Contacts"/>
                            <Button x:Name="HistoryButton" Style="{StaticResource NavButton}" Content="Call history"/>
                            <Button x:Name="MspButton" Style="{StaticResource NavButton}" Content="ConnectWise"/>
                            <Button x:Name="VoicemailButton" Style="{StaticResource NavButton}" Content="Voicemail"/>
                            <Button x:Name="ParkingButton" Style="{StaticResource NavButton}" Content="Parking"/>
                            <Button x:Name="RecordingsButton" Style="{StaticResource NavButton}" Content="Recordings"/>
                        </StackPanel>
                    </ScrollViewer>
                    <StackPanel Grid.Row="1" Margin="0,8,0,0">
                        <Button x:Name="MessagesButton" Style="{StaticResource NavButton}" Content="More"/>
                        <Button x:Name="SettingsButton" Style="{StaticResource NavButton}" Content="Settings"/>
                    </StackPanel>
                </Grid>

                <StackPanel x:Name="SidebarStatus" Grid.Row="3" Margin="0,14,0,0">
                    <TextBlock Text="PHONE STATUS" Foreground="{StaticResource Muted}" FontSize="9" FontWeight="SemiBold"/>
                    <StackPanel Orientation="Horizontal" Margin="0,8,0,0">
                        <Ellipse x:Name="StatusDot" Width="9" Height="9" Fill="{StaticResource Warning}" Margin="0,4,8,0"/>
                        <TextBlock x:Name="StatusText" Text="Checking" Foreground="{StaticResource Muted}" FontSize="11"/>
                    </StackPanel>
                    <TextBlock x:Name="VersionText" Text="CallBridge" Foreground="#8796A4" FontSize="10" Margin="0,12,0,0"/>
                </StackPanel>
            </Grid>
        </Border>

        <Grid Grid.Column="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="74"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <Border BorderBrush="#D7E6ED" BorderThickness="0,0,0,1" Background="#FBFEFF">
                <Grid Margin="28,0">
                    <StackPanel VerticalAlignment="Center">
                        <TextBlock x:Name="PageTitle" Text="Phone" FontWeight="SemiBold" FontSize="19"/>
                        <TextBlock Text="Softphone workspace for SIP calling, ConnectWise context, and technician workflows" Foreground="{StaticResource Muted}" FontSize="11" Margin="0,4,0,0"/>
                    </StackPanel>
                    <WrapPanel HorizontalAlignment="Right" VerticalAlignment="Center">
                        <ComboBox x:Name="ProviderBox" Width="190" MinWidth="150" Height="36" Margin="0,0,10,0"/>
                        <Button x:Name="RegisterButton" Content="Register" Background="#0E86BC" Foreground="White" BorderBrush="#0E86BC"/>
                    </WrapPanel>
                </Grid>
            </Border>

            <Grid x:Name="ContentHost" Grid.Row="1" Margin="28">
                <Grid x:Name="PhoneView">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition x:Name="DialerColumn" Width="376" MinWidth="300"/>
                        <ColumnDefinition x:Name="PhoneGapColumn" Width="20"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <Border Style="{StaticResource PanelBorder}" Padding="24">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="*"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>

                            <Border BorderBrush="#BFDDE8" BorderThickness="1" CornerRadius="8" Padding="12">
                                <Border.Background>
                                    <LinearGradientBrush StartPoint="0,0" EndPoint="1,0">
                                        <GradientStop Color="#EAF8FF" Offset="0"/>
                                        <GradientStop Color="#EFFBFA" Offset="1"/>
                                    </LinearGradientBrush>
                                </Border.Background>
                                <Grid>
                                    <TextBlock Text="LIVE PHONE" Foreground="{StaticResource Muted}" FontSize="10" FontWeight="SemiBold" HorizontalAlignment="Left"/>
                                    <TextBlock x:Name="CallStateText" Text="READY" Foreground="{StaticResource Brand}" FontSize="10" FontWeight="SemiBold" HorizontalAlignment="Right"/>
                                </Grid>
                            </Border>

                            <TextBox x:Name="DialText" Grid.Row="1" TextAlignment="Center" FontSize="30" Margin="0,18,0,16" BorderThickness="0,0,0,1" FontWeight="SemiBold"/>

                            <UniformGrid x:Name="DialPad" Grid.Row="2" Columns="3" HorizontalAlignment="Center" VerticalAlignment="Center">
                                <Button Style="{StaticResource DialButton}" Content="1"/>
                                <Button Style="{StaticResource DialButton}" Content="2 ABC"/>
                                <Button Style="{StaticResource DialButton}" Content="3 DEF"/>
                                <Button Style="{StaticResource DialButton}" Content="4 GHI"/>
                                <Button Style="{StaticResource DialButton}" Content="5 JKL"/>
                                <Button Style="{StaticResource DialButton}" Content="6 MNO"/>
                                <Button Style="{StaticResource DialButton}" Content="7 PQRS"/>
                                <Button Style="{StaticResource DialButton}" Content="8 TUV"/>
                                <Button Style="{StaticResource DialButton}" Content="9 WXYZ"/>
                                <Button Style="{StaticResource DialButton}" Content="*"/>
                                <Button Style="{StaticResource DialButton}" Content="0 +"/>
                                <Button Style="{StaticResource DialButton}" Content="#"/>
                            </UniformGrid>

                            <Button x:Name="CallButton" Grid.Row="3" Content="Call" Foreground="White" BorderThickness="0" FontWeight="SemiBold" Padding="15" Margin="0,18,0,0" Background="#31B78D"/>

                            <UniformGrid Grid.Row="4" Columns="4" Margin="0,10,0,0">
                                <Button x:Name="MuteButton" Content="Mute" Margin="3"/>
                                <Button x:Name="HoldButton" Content="Hold" Margin="3"/>
                                <Button x:Name="TransferButton" Content="Transfer" Margin="3"/>
                                <Button x:Name="DtmfButton" Content="DTMF" Margin="3"/>
                            </UniformGrid>
                        </Grid>
                    </Border>

                    <Grid Grid.Column="2">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="18"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>

                        <UniformGrid Columns="3">
                            <Border Style="{StaticResource MetricCard}" Margin="0,0,10,0">
                                <StackPanel>
                                    <TextBlock Text="Registration" Foreground="{StaticResource Muted}" FontSize="10" FontWeight="SemiBold"/>
                                    <TextBlock x:Name="RegistrationMetric" Text="Not registered" FontSize="19" FontWeight="SemiBold" Margin="0,7,0,0"/>
                                </StackPanel>
                            </Border>
                            <Border Style="{StaticResource MetricCard}" Margin="0,0,10,0">
                                <StackPanel>
                                    <TextBlock Text="Active call" Foreground="{StaticResource Muted}" FontSize="10" FontWeight="SemiBold"/>
                                    <TextBlock x:Name="ActiveCallMetric" Text="None" FontSize="19" FontWeight="SemiBold" Margin="0,7,0,0"/>
                                </StackPanel>
                            </Border>
                            <Border Style="{StaticResource MetricCard}">
                                <StackPanel>
                                    <TextBlock Text="Connector API" Foreground="{StaticResource Muted}" FontSize="10" FontWeight="SemiBold"/>
                                    <TextBlock x:Name="ApiMetric" Text="Unknown" FontSize="19" FontWeight="SemiBold" Margin="0,7,0,0"/>
                                </StackPanel>
                            </Border>
                        </UniformGrid>

                        <Border Grid.Row="2" Style="{StaticResource PanelBorder}" Padding="22">
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="*"/>
                                </Grid.RowDefinitions>
                                <DockPanel>
                                    <TextBlock x:Name="PanelHeading" Text="Live directory" FontWeight="SemiBold" FontSize="20"/>
                                    <Border DockPanel.Dock="Right" Background="#EEF7FB" BorderBrush="#D0E7EF" BorderThickness="1" CornerRadius="8" Padding="10,5">
                                        <TextBlock Text="Double-click to dial" Foreground="{StaticResource BrandDark}" FontSize="11" FontWeight="SemiBold"/>
                                    </Border>
                                </DockPanel>
                                <ListBox x:Name="MainList" Grid.Row="1" Margin="0,14,0,0" BorderThickness="0" Background="Transparent" ItemTemplate="{StaticResource RowItemTemplate}" ItemContainerStyle="{StaticResource SoftListItem}"/>
                            </Grid>
                        </Border>
                    </Grid>
                </Grid>

                <Grid x:Name="ListView" Visibility="Collapsed">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="18"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    <DockPanel LastChildFill="False">
                        <StackPanel DockPanel.Dock="Left">
                            <TextBlock x:Name="ListHeading" Text="Contacts" FontSize="31" FontWeight="SemiBold"/>
                            <TextBlock x:Name="ListSubheading" Text="People and extensions" Foreground="{StaticResource Muted}" Margin="0,6,0,0"/>
                        </StackPanel>
                        <WrapPanel DockPanel.Dock="Right" HorizontalAlignment="Right" VerticalAlignment="Center" MaxWidth="800">
                            <Button x:Name="SyncConnectWiseButton" Content="Sync ConnectWise" Padding="14,8" Margin="0,0,8,8"/>
                            <Button x:Name="OpenCompanyButton" Content="Open company" Padding="14,8" Margin="0,0,8,8"/>
                            <Button x:Name="CreateTicketButton" Content="New ticket" Padding="14,8" Margin="0,0,8,8"/>
                            <Button x:Name="CallNoteButton" Content="Call notes" Padding="14,8" Margin="0,0,8,8"/>
                            <Button x:Name="ExportHistoryButton" Content="Export CSV" Padding="14,8" Margin="0,0,8,8"/>
                            <Button x:Name="AddContactButton" Content="Add contact" Padding="14,8" Margin="0,0,8,8"/>
                            <Button x:Name="EditContactButton" Content="Edit" Padding="14,8" Margin="0,0,8,8"/>
                            <Button x:Name="DeleteContactButton" Content="Delete" Padding="14,8" Margin="0,0,8,8"/>
                            <Button x:Name="ImportContactsButton" Content="Import CSV" Padding="14,8" Margin="0,0,8,8"/>
                            <Button x:Name="ClearContactsButton" Content="Clear imports" Padding="14,8" Margin="0,0,8,8"/>
                            <Button x:Name="RefreshRowsButton" Content="Refresh" Padding="14,8" Margin="0,0,12,8"/>
                            <TextBox x:Name="SearchBox" Width="240" Height="38" VerticalAlignment="Center"/>
                        </WrapPanel>
                    </DockPanel>
                    <Border Grid.Row="2" Style="{StaticResource PanelBorder}" Padding="18">
                        <ListBox x:Name="RowsList" BorderThickness="0" Background="Transparent" ItemTemplate="{StaticResource RowItemTemplate}" ItemContainerStyle="{StaticResource SoftListItem}"/>
                    </Border>
                </Grid>
            </Grid>
        </Grid>
    </Grid>
</Window>
